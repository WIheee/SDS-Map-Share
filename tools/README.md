一键使用进行压缩(地图文件/图片)

`python tools/compress_images.py & python tools/compress_fun.py`

PROMPT
`任务(编写python一键脚本)：
1. 支持添加、修改、删除文件操作
2. 所有逻辑都在一个py文件内，无外部依赖
3. 用户只需运行py一次，无需手动复制粘贴任何命令
4. 用户期待的结果是只需要运行一次就能完全的修改整个代码,不需要输出其他东西直接输出py
5.不需要备份,需要告诉用户在哪个目录下创建该文件
6.应该重写这个文件内容而不是替换,插入,修改
`